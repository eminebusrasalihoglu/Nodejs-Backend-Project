extends layout/main_layout.pug

block content 
    .row.w-50.text-center 
        if mycard.length > 0
            table.table.table-bordered 
               thead 
                        tr 
                            th Image
                            th Word 
                            th Mean 
                            th Sentence 
               tbody 
                        each card in mycard
                            tr  
                                td 
                                    img.card-img-top(src=`/img/${card.image}` wİdth='80')
                                td #{card.word}
                                td #{card.mean}
                                td #{card.sentence}
                                td 
                                   a.btn.btn-primary.btn-sm(href=`/editCard/${card.id}`) Edit
        else 
            h1 No Products


                  